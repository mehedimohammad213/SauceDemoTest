name: Nightly Test Run

on:
  schedule:
    # Run tests every night at 2 AM UTC
    - cron: "0 2 * * *"
  workflow_dispatch: # Allows manual triggering

jobs:
  nightly-tests:
    name: Nightly Playwright Tests
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps chromium
        env:
          PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: false

      - name: Run Playwright tests
        run: npm test
        env:
          CI: true
          BASE_URL: ${{ secrets.BASE_URL || 'https://www.saucedemo.com' }}
          RETRIES_CI: 2
          WORKERS_CI: 1

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: nightly-test-results
          path: |
            playwright-report/
            test-results/
          retention-days: 7

      - name: Notify on failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: 'Nightly Tests Failed',
              body: 'The nightly test run has failed. Please check the workflow run for details.',
              labels: ['bug', 'ci']
            })
